---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/kustomization-kustomize-v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app plex2
spec:
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  components:
    - ../../../../components/zeroscaler
    - ../../../../components/gpu
    - ../../../../components/kopiur
  dependsOn:
    - name: kopiur
      namespace: system
    - name: rook-ceph-cluster
      namespace: rook-ceph
  interval: 1h
  path: ./kubernetes/apps/default/plex2/app
  postBuild:
    substitute:
      HOSTNAME: plex2.ctec.run
      APP: *app
      IP: 10.6.0.18
      PVC_CAPACITY: "50Gi"
      APP_UID: "2000"
      APP_GID: "2000"
      OLD_NS: default
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: default
  timeout: 30m
  wait: false
