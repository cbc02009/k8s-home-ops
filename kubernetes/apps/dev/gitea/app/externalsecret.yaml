---
# yaml-language-server: $schema=https://kubernetes-schemas.kokoro.wtf/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: gitea
  namespace: dev
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: gitea-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        # Gitea
        username: "{{ .ADMIN_USER }}"
        password: "{{ .ADMIN_PASS }}"
        email: "{{ .ADMIN_EMAIL }}"
        minioAccessKeyId: "{{ .minioAccessKeyId }}"
        minioSecretAccessKey: "{{ .minioSecretAccessKey }}"
        ENV_TO_INI__DATABASE__PASSWD: "{{ .ENV_TO_INI__DATABASE__PASSWD }}"
        ENV_TO_INI__DATABASE__USER: "{{ .ENV_TO_INI__DATABASE__USER }}"
        # Postgres Init
        POSTGRES_DB: "gitea"
        POSTGRES_HOST: ${POSTGRES_HOST}
        POSTGRES_SUPER_PASS: "{{ .POSTGRES_SUPER_PASS }}"
        POSTGRES_PASS: "{{ .ENV_TO_INI__DATABASE__PASSWD }}"
        POSTGRES_USER: "{{ .ENV_TO_INI__DATABASE__USER }}"
  dataFrom:
    - extract:
        key: gitea
    - extract:
        key: postgres
