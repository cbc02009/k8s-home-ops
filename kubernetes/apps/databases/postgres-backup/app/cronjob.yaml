---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: postgres-backup
  namespace: databases
spec:
  schedule: "0 17 * * *"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          automountServiceAccountToken: false
          enableServiceLinks: false
          securityContext:
            runAsUser: 65534
            runAsGroup: 1010
            fsGroup: 1010
            supplementalGroups:
              - 65541

          containers:
            - name: postgres-backup
              image: docker.io/prodrigestivill/postgres-backup-local:15@sha256:b3905c58ab8c1fb7596e2f90f059c65d61c2a813232e6eca4a3f5ce019bfbd61
              imagePullPolicy: IfNotPresent
              command:
                - "/backup.sh"

              env:
                - name: POSTGRES_HOST
                  value: ${POSTGRES_HOST}
                - name: INIT_POSTGRES_DBNAME
                  value: "authelia, gitea, grafana, home_assistant, immich, nextcloud, postgres, prowlarr_main, radarr_main, vaultwarden"
                - name: INIT_POSTGRES_USER
                  valueFrom:
                    secretKeyRef:
                      name: postgres-backup-secret
                      key: username
                - name: POSTGRES_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: postgres-backup-secret
                      key: password
                - name: BACKUP_KEEP_DAYS
                  value: "7"
                - name: BACKUP_KEEP_WEEKS
                  value: "4"
                - name: BACKUP_KEEP_MONTHS
                  value: "6"
                - name: BACKUP_KEEP_MINS
                  value: "7200"
                - name: POSTGRES_EXTRA_OPTS
                  value: "-b -C -Z6"
              volumeMounts:
                - name: nas-backups
                  mountPath: /backups

          restartPolicy: OnFailure

          volumes:
            - name: nas-backups
              nfs:
                server: "nas.ctec.run"
                path: /tank/backup/kubernetes/apps/postgres
