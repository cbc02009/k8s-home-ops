---
# yaml-language-server: $schema=https://kubernetes-schemas.kokoro.wtf/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: &name radarr
  namespace: organizarrs
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: radarr-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        # Prowlarr
        RADARR__API_KEY: "{{ .RADARR__API_KEY }}"
        RADARR__POSTGRES_USER: "{{ .RADARR__POSTGRES_USER }}"
        RADARR__POSTGRES_PASSWORD: "{{ .RADARR__POSTGRES_PASSWORD }}"
        # Postgres Init
        POSTGRES_DB: "radarr_main radarr_log"
        POSTGRES_HOST: ${POSTGRES_HOST}
        POSTGRES_SUPER_PASS: "{{ .POSTGRES_SUPER_PASS }}"
        POSTGRES_PASS: "{{ .RADARR__POSTGRES_PASSWORD }}"
        POSTGRES_USER: "{{ .RADARR__POSTGRES_USER }}"
  dataFrom:
    - extract:
        # RADARR__API_KEY, RADARR__POSTGRES_USER, RADARR__POSTGRES_PASSWORD
        key: radarr
    - extract:
        key: postgres
