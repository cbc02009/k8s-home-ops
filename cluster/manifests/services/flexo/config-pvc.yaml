---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: flexo-data-v1
  namespace: services
  labels:
    app.kubernetes.io/name: &name flexo
    app.kubernetes.io/instance: *name
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 10Gi

  storageClassName: tank-iscsi-csi
