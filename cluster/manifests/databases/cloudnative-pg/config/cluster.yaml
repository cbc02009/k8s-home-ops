---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgres
  namespace: databases
spec:
  instances: 3
  primaryUpdateStrategy: unsupervised
  storage:
    size: 30Gi
    storageClass: tank-iscsi-csi
  superuserSecret:
    name: postgres-superuser
  backup: {}
