---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: databases-postgres-secret
  namespace: flux-system
spec:
  dependsOn:
    - name: kube-system-onepassword-connect
  interval: 30m
  path: "./cluster/manifests/databases/postgres-secret"
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: k8s-home-ops
